package com.singularity.clover.entity;

import android.view.View;

public interface EntityViewModel {
	View initView();
	View changeModel(int model,View srcView);
	View entityToView(Persisable e,View layout);
	Persisable viewToEntity(View layout,Persisable e);
	void updateView(View v);
}
